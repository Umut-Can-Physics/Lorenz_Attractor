[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lorenz-attractor"
version = "0.1.0"
description = "Lorenz system solver and plotting tools"
authors = [{name = "Unknown"}]
requires-python = ">=3.8"

[project.optional-dependencies]
plot = ["matplotlib"]
